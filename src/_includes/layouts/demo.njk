<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{{ demo.title }}</title>
    <style>
      {% if demo.css %}
        {{demo.css | safe}}
      {% endif %}
      {% if demo.displayedCss %}
        {{demo.displayedCss | safe}}
      {% endif %}
    </style>
  </head>
  <body>
    <main id="demo-content" title="{{ demo.title }}">
      {{ demo.html | safe }}
    </main>
    {% if demo.javascript %}
      <script>
        {{ demo.javascript | safe }}
      </script>
    {% endif %}
  </body>
</html>
